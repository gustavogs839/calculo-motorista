Regras do Firestore (Substitua no Console do Firebase):

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Apenas usuários autenticados podem escrever seus próprios dados
    match /transacoes/{document=**} {
      allow read, write: if request.auth.uid == resource.data.userId;
      allow create: if request.auth.uid == request.resource.data.userId;
    }
  }
}

PASSOS PARA ATUALIZAR:
1. Acesse: https://console.firebase.google.com/
2. Selecione o projeto "motorista-app-a0d2e"
3. Vá para "Firestore Database"
4. Clique em "Rules" (abas no topo)
5. Cole as regras acima
6. Clique em "Publicar"

IMPORTANTE:
- Antes de publicar, você precisa adicionar userId aos documentos existentes
- Crie um script para fazer isso (veja abaixo)
